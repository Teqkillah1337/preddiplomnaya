<Window x:Class="PCInventory.CompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Сравнение конфигураций" 
        Height="550" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#7B1FA2" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <TextBlock x:Name="txtTitle" 
                       Text="Сравнение конфигураций" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Информация о датах -->
        <Border Grid.Row="1" Background="#FFF3E0" CornerRadius="6" Padding="10" Margin="0,0,0,15">
            <TextBlock x:Name="txtDatesInfo" 
                       FontSize="12" 
                       Foreground="#333"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Список различий -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           x:Name="txtSummary" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,10"
                           TextWrapping="Wrap"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="lstDifferences">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" Margin="5" CornerRadius="4" Background="#FFF3E0">
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="12" FontFamily="Consolas"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button x:Name="btnExportDiff" Content="📄 Экспортировать отчет" Style="{StaticResource PrimaryButton}" Width="193"/>
            <Button x:Name="btnClose" Content="Закрыть" Style="{StaticResource SecondaryButton}" Width="120"/>
        </StackPanel>
    </Grid>
</Window>